# Corner close geometry and settings glyphs

User accepts the previous refinement: no opening flash, otherwise working well;
a closing flash remains and both overflow settings icons need distinguishing.

A QML signal test reproduces source height [0, full] during warm Quick Pad close.
Cached=true was already set, but height bindings considered only active state.
Use retained corner geometry for both input heights; visibility/ink still depend
on paired and the native transaction gate. Do not modify refresh waveforms.

Overflow Quick Pad now references the accepted native task-list glyph. Companion
adds the same native baby-notebook motif in the foldout icon slot, using native
padding/sizing rather than proportional placement across a text row.

Preserve both settings stores and installed highlighter in the guarded update.
No synthetic tablet interactions; actual close-flash improvement is user feedback.
