# Lifecycle capsule reviewed and staged, not activated

## Status

The pending ordinary trial052500 readback was completed in this session: four
correctly attributed saved shapes, normal UI99661/Dates14463, zero restarts,
protected base/settings/policies unchanged, root read-only and no test ownership.

New trial `20260923T103000Z-1` is staged **only on the Mac** at
`build/probe-20260923T103000Z-1/`. ReManager independently cleared its exact bytes
for one bounded, always-reverting run on two fresh disposable notes. The first
SSH connection of the upload command timed out, so the chained upload/prepare
commands never ran. No new remote stage, recovery backup, UI restart or native
test was performed in this session. Subsequent direct retries timed out; the
full LAN TCP22 scan found only a known non-Pro host, with no authentication to it.

Local one-run staging clearance is consumed to prevent regeneration. The unused
frozen capsule can proceed after fresh live identity/base/policy checks and a new
Mac-verified recovery archive. It is not an everyday installer or sleep clearance.

## Exact frozen capsule

| Artifact | SHA256 |
| --- | --- |
| Complete11-file manifest | `1d0bd6381c25f8c2d8787f8cc13b0556fa31ebd155b31e7ee03a8c6c7a0b53b9` |
| NativeHost.qml | `26fd85c01ee37869a38dd1fa22d0cf27ed352e79de2ab733606e6ce930d54d56` |
| Companion QMD | `71824e22e9a2a5d05a2991763bc4431017fb0c2789075637c1cf24450a3442f6` |
| Controller | `1a4b2c6adc9d6e39df7e03d7440efabea721b3456aadf48f4c1d2b93f6fb4aef` |
| Accepted base manifest | `5fe7e2ec3291efa692c90df769ea521d9e399d3da6e7448f9a9071caca71652d` |

The remaining ruler/store/core/bootstrap/marker helpers are byte-identical to the
qualified ordinary capsule. The controller changes only completion receipts and
the post-ink lifecycle poll budget; its180-second independent deadline, cgroup
ownership, release and recovery logic remain intact. A reverse-diff regression
reconstructs the old successful controller exactly.

## Review corrections and test sequence

Before activation, independent review caught two real defects:

- The bar Redo string replacement silently missed its semicolon-free anchor.
  Builder now asserts the unique anchor and the composed native cnAction contains
  an actual Redo case in all3 load orders. The test uses that bar action.
- Diagnostic pen eligibility required two views even after secondary close.
  `probeSolePrimary()` admits only the exact created primary view/ID/scene/
  controller while the close/reopen steps11/12 are active and no secondary exists.
  Step12 preserves that eligibility during the reopen drain. Native freshness
  checks are unchanged; no empty-publication bypass was added.

The frozen trial creates2 labelled notes, writes the original4 shapes at½/⅔,
undoes/redoes in each, selects eraser then pen, adds one page per notebook and
returns to each original page, closes/reopens the secondary, then tucks/reveals,
cancels the chooser and parks through autosave before automatic restoration.
Readback must prove all4 original shapes remain on their original pages and the
2 newly added pages are blank. Every release/visual/physical-boundary claim remains
separate. Before later local availability edits, this cohort passed384 Node
cases (7 historical skips),67 Qt cases,31 saved-file fixtures and33 composed
resources in each of3 orders.

## Later local source, not in the frozen capsule

The ordinary adapter now stops in an owned `suspended` park if no primary input
is visible, rather than trying to finish with an empty candidate cache. Wake
resumes fresh geometry/publication; a guarded stock primary-open can continue
the same park from the library. Landscape single-document stock input can resume
without reopening the portrait companion. The pointer shield permits library
interaction but retains any existing pressed grab until its own release.

This does not change power settings or the native core. Sleep during a pending
asynchronous add still fails closed under the existing strict callback identity
rules; actual pre-sleep ordering and native wake remain unqualified. Offline
inspection found that stock sleep-save work uses a separate DocumentWorker,
not the PenInputThread; this is useful evidence, not a live sleep-safety proof.
Do not replace the reviewed capsule with this later source without new review.
Final local checks for the later source:387 Node cases passed,7 historical skips;
68 offscreen Qt cases passed;31 saved-file fixtures passed; normal composition
produced33 resources in each of3 orders. The frozen capsule's11 hashes still match.

## Resume

1. Strict-key connect to Ferrari; verify exact3.29.0.148 identity and accepted base.
2. Use the existing local frozen stage, first checking its complete manifest.
3. Upload to a fresh remote private stage; run exact controller `prepare` only.
4. Copy/verify the new recovery archive on Mac, then mark it verified remotely.
5. Start the bounded owner once, let its watchdog restore base, and independently
   verify recovery plus the exact2 newly created saved documents.
6. Continue the separate sleep/boundary/extension qualification; no Move changes.
