# Screen sharing is user-controlled

The user explicitly rejected the privacy block that refused another notebook
while native sharing or RMStream was active. Ordinary Companion and Quick Pad
must behave the same while broadcasting, without another confirmation dialog.

Remove only the six interactive host checks. Keep historical diagnostic
restrictions, document eligibility, worker parking, pen ownership, native saving
and recovery unchanged. No capture helper or automatic demonstration is enabled.
Update the public usage notes and test sharing-active pick/toggle/corner paths.
