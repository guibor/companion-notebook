#+title: Companion Notebook

* Objective
Read and write without losing context or compromising native handwriting.

* Publication (2026-09-25)
- Publish current Quick Pad refinements on the existing Pro branch and document
  the actual installed workflow, settings and limitations in README.
- Documentation/publishing only; do not interrupt the tablet session.
** Next steps
- Collect user feedback on the remaining late flash in the installed adjustment;
  do not claim its elimination from local tests alone.

* Quick Pad warm toggles (2026-09-24)
- User accepts192600 toolbar/reuse behavior; remove a remaining extra late flash
  without altering working interaction. Avoid redundant native width-fit writes.
- Keep all source toolbar buttons and their positions stable when the pad opens.
  Hidden secondary toolbars must not reconfigure the shared toolbar provider.
- User accepts192000 size, zoom and scrolling; remaining defect is flashing.
- Keep the working geometry and pen handling. Reuse the loaded native pad for
  toolbar tuck/reveal rather than reload every time, with autosave alive.
- Fully retire on source change, sleep/unavailability, pad/settings change or
  returning to an open normal Companion. Revalidate last-page identity on reveal.
** Changed requirement
- Toggling off no longer always destroys the native pad; it normally hides it.
  No notebook data is cached by custom code, only the existing native view.
** Next steps
- Guarded installation and user feedback on repeat-toggle flashes; no pen trials.

* Quick Pad accepted refinement (2026-09-24)
- User confirms corner handwriting works. Preserve both-document native ink.
- Default Wide pad: two-thirds width, one-third height, flush bottom-right.
- Settings offer Compact (half width/third height), Wide, and Roomy (two-thirds
  width/half height), with bottom-left/right preference and existing-pad Apply.
- Always initially fit width, never height; preserve saved vertical context.
- Allow scrolling into blank space beneath the page to extend handwritten notes.
- Reduce toggling flashes without global refresh-policy or pen-handler changes.
** Changed requirements
- The fixed half-by-half pane and whole-page fit are superseded by presets and
  width fit. A long page must not force tiny writing. Thin edges/icon toggle stay.
** Immediate next steps
- Install the guarded revision with preserved settings; user assesses remaining
  flashes and native extension. No automatic tablet pen trials.

* Quick Pad (2026-09-24, offline candidate)
- Same repository/application; a distinct toolbar button with a pad/task icon.
- A device-wide configurable notebook, separate from per-document companion pairs.
- First tap opens Recent/Favorites notebook selection; later taps toggle the pad.
- Bottom-right by default, configurable bottom-left. Roughly half width and half
  height (quarter of the screen), inset to keep either toolbar side unobstructed.
- Open the notebook's current last page every time and fit its full bounds before
  publishing pen input; preserve native handwriting and shared writing tools.
- Small header with settings and close; no resizing/dragging in this first version.
- Reuse one secondary native view slot. Temporarily replace an open bottom split;
  close restores it and never overwrites ordinary pairing metadata.
- Prepare offline only: user explicitly says the tablet is unavailable. No device
  discovery, SSH, runtime changes, firmware writes or Move work in this session.
- Conservative initial candidate: native source handwriting is paused while the
  pad is visible, avoiding an unverified L-shaped overlapping pen region. Source
  navigation remains available; this is not a general document-edit lock. The
  header says "pen here". Both-pane writing for this geometry is deferred until
  separate native routing proof; ordinary bottom-split behavior is unchanged.
- Native scaled-ink, pen-boundary and scrolling acceptance remain hardware checks,
  not outcomes implied by local Qt/Node mocks or QML composition.

* Accepted requirements (2026-09-21)
- Portrait-first bottom pull-out writable margin overlays the main document.
- Main viewport and page scale do not shrink as the margin is pulled.
- Layout controls must be pictorial, with no fraction labels. Support a small,
  balanced, large and companion-only presentation, plus source-only/closed.
  Exact control grouping is a UX proposal pending agreement. Remember the last
  split size per pair; reopen restores it. No live dragging is required.
- Two independently scrollable documents; write directly in either visible pane.
- Pull out the notebook and start writing without an activation tap. Native pen
  hit-testing chooses the document at stroke start and keeps it until pen-up.
- The native toolbar must remain visible and usable above/outside the companion.
  One writing-tool selection (pen, color, thickness, eraser) applies to both panes;
  no manual pane selection is required to share the tool. Document operations
  remain scoped to one notebook; their target behavior is specified separately.
- The middle separator is a hairline only, with no controls or labels at all.
  Layout pictures and close live in the existing document toolbar menu, following
  the RMHacks split-document pattern. No floating reopen button on the page.
- Pairings belong to the primary notebook or PDF; no same-document pair in v1.
- Tucking preserves positions and pairing; detaching never deletes documents.
- No layout/focus/document changes during a stroke; gestures keep their owner.
- Prototype is local/offline. Native ink must use native controllers and saves.
- Pro only initially; no changes to Move or shared servers.

* Requirements changed
- Further icon feedback: remove the composed task-list drawing. Use the native
  formatting_checkbox task-list glyph directly, without a custom surrounding box.
- User selected task-list option C: closed native-style frame, two unchecked
  boxes and short rules. Replace the rejected screen-sharing composition below;
  no dots, open outlines, floating checkmark or overlapping notebooks.
- Quick Pad icon: replace the custom checklist/corner drawing with the native
  screen-sharing frame and native checkmark in place of its arrow. Do not reuse
  Companion's overlapping notebooks. Keep native selected-state contrast.
- Add a global corner-pad mode alongside the existing per-document bottom split.
  The two modes are mutually exclusive, not three simultaneously writable views.
- Icon-only refinement: retain the full-size native notebook; overlap a baby
  instance of the same notebook at its corner, replacing the square/minus badge.
  Add clearly labeled public UI mockups to the README and Reddit announcement.
- Replace the original custom notebook drawing with the actual native notebook
  icon plus a small companion badge. Picker tabs are Recent and Favorites;
  favorites are independently queried, not restricted to recent documents.
- User requested public repository publication and a Reddit announcement after
  this update. Review public history for secrets/firmware; preview post text per
  the Reddit posting workflow before submission.
- User accepted210500 and requested commit/push. Next refinement reverses the
  endpoint arrows; switching to the companion truly swaps document roles, so
  subsequent small/medium/half panes show the previous primary. This replaces
  the old fullReturn behavior that kept original source roles fixed.
- Replace the full-screen chooser with a polished popup above the toolbar, a
  scrollable recent list and a dedicated monochrome paired-notebook icon.
  Full-library browsing is lower priority and remains deferred.
- Subsequent size refinement: splits are quarter, three-eighths (default), half;
  no three-quarter pane. Source-only and full-companion endpoints remain. Existing
  three-quarter settings normalize to half. Icons remain unlabeled by fractions.
- Feedback2026-09-23: Companion picker lives above Dates on the full toolbar;
  size icons remain in three dots above Dispatch, available before pairing.
  The ordered spectrum ends in source-only and companion-only document icons,
  replacing X/full and removing the redundant Turn off Companion menu item.
  First size choice opens the picker if unpaired. A companion defaults back to
  its source unless it already has an explicitly saved companion. Fix all sizes.
- Screenshot clarification2026-09-23 supersedes even the proposed minimal middle
  control strip: two-pixel separator only; all layout/close controls in the native
  menu. Keep one unobstructed native toolbar alongside both panes. User authorized
  implementation and installation of this revision, not automated pen trials.
- Hands-on feedback2026-09-23: companion covers the vertical toolbar; native
  toolbar choices do not apply to the companion. Fix these as product defects.
  Shared writing-tool selection replaces pane-specific tool selection. Pictorial
  layout choices replace⅓/½/⅔ labels, with source-only and companion-only ends.
  The busy middle toolbar is removed. Proposed UX: four layout pictograms
  (quarter, half, three-quarter, full) plus X as the source-only/close action;
  preserve pairing on close, keep Remove pairing in the document menu.
- On2026-09-23 the user explicitly stopped further automated verification and
  requested hands-on use with their own feedback. Deliver a manually activated,
  plainly experimental Pro pilot with a visible off switch and normal-runtime
  fallback. This replaces further pre-pilot automated trials, not a claim that
  remaining release gates passed. No injected input, test-note creation, firmware,
  boot persistence, Move deployment or recurring-install entry in this pilot.
- On 2026-09-22 the user dropped live dragging and requested fixed fractions,
  optionally chosen on a ruler. This supersedes continuous motion, divider-drag
  and high-speed-video fluidity requirements. Native ink handoff, clipping and
  preservation requirements still apply to every discrete geometry change.
- Earlier proposal displaced/resized the main viewport: replaced by slide-over.
- Earlier automatic focus was replaced by explicit tap-to-select; on 2026-09-22
  the user superseded that decision again: direct writing in either visible pane
  is preferred, particularly after pulling out the notebook. No select-only pen tap.
- Same-notebook different locations: excluded from v1.
- Landscape in v1: deferred. Later landscape should fit scaled documents to equal halves.
- Earlier divider-only preview fallback was rejected; the later fixed-size
  requirement supersedes the live-fluid-movement gate, not native ink safety.
- Design-only phase: user authorized implementation, with original device safety constraints.
- After the structural trial, the user explicitly requested continued work until
  usable unless technically infeasible or very risky. This supersedes the earlier
  overnight stop decision; it does not authorize unsafe pen routing or firmware edits.
- On 2026-09-22 the user challenged the stroke-safety stop and explicitly asked
  to try both panes writable without a selection tap. Continue with an isolated,
  fixed-layout native-ink experiment on two new disposable notes; retain all
  personal-document release gates and the no-firmware/no-bricking-risk boundary.

* Validation and release decision
- Desktop controller and QML tests must pass, including ruler taps and rejecting
  accidental drags, pen/scroll locks, and selected-size restoration.
- Hardware must prove independent rendering, clipping, pen ownership and native durability.
- Base 3.29 XOVI/QRR qualification precedes any native probe.
- Any lost, duplicate or wrong-document ink blocks release.
- Verify discrete size changes and native repaint/clipping on the Pro.
- No claim of native support or inclusion in reinstalls before these gates pass.

* Next steps
0. Quick Pad183200 installed on Pro3.29.0.148, UI372703/guard372402 active.
   First182400 reset pairing defaults;183200 isolates the settings category and
   restores all three original pairings with exact post-startup field hash match.
   Native task-list icon; hand over for user feedback, no automated pen tests.
0. Icon revision installed with183200: stock formatting_checkbox, no overlays.
0. Quick Pad offline implementation, tests and package are complete:
   20260924T120500Z-1, 18 focused Node tests, 12 Qt Quick Pad checks, menu/picker
   regressions and 33 QML resources parsed in each of three dependency-respecting
   orders. Normal pilot output is byte-identical to installed234000; opt in with
   CN_QUICK_PAD=1. No tablet contact or deployment. Next device work
   must start with current identity/firmware/base/backup checks and a bounded
   scaled-input check, not an automatic rollout or a new firmware adaptation.
0.234000 installed with big/baby native notebook icon; UI185793 host-ready.
   Public README mockups committed/pushed. No picker, layout, input, pairing or
   Move behavior changes. Reddit image/caption approval remains pending.
0.230000 installed: native notebook icon with badge, Recent/Favorites popup,
  reversed arrows and true role swap. Host ready onUI177343;
  owner176282/watch177031 active. User hands-on feedback next; no input automation.
  Implementation committed asd4b0290 and pushed to the public repository
  https://github.com/guibor/companion-notebook (current branch is default).
  Approved portrait-only announcement published with Self-Promotion flair:
  https://www.reddit.com/r/RemarkableTablet/comments/1wobgko/i_built_a_writable_splitscreen_companion_for/
0. Revised210500 is active onUI166334: full-toolbar pairing, size-first picker,
   source/quarter/three-eighths/half/companion spectrum and reverse pair defaults.
   Host ready with ink/settings true; user feedback next, no input automation.
   Ordering helper removed after210000 reported unsupported stackBefore. Size
   strip stays in three dots; exact placement above Dispatch deferred.
0. Pilot190000 automatically restored baseUI153378 after a false sleep-inclusive
   heartbeat timeout. Corrected200000 counts completed missed polls, retaining
   crash recovery and pairings; deployed without automated pen interactions.
0. Hairline/shared-toolbar pilot200000 is installed on ProUI155746. Native host
   ready with writing/settings enabled; session154630 and fallback155390 active.
   Previous pair settings carried forward. User feedback next; no automated pen
   trials. Menu-only layout pictures/close, shared writing settings, no middle UI.
0. Historical first pilot170000 was installed on ProUI132308, with native
   writing/settings ready; it has now been replaced by190000 above. Do not run
   more automated tablet trials or manipulate the
   tablet while the user tries it. Turn off Companion restores the normal base.
   No full-release claim, Move deployment or recurring-install entry.
1. Lifecycle103000 is now COMPLETE: native history/tools/page-add/return and
   close/reopen passed on UI104747; exact saved-file check passed all4 shapes and
   new blank pages. Independent recovery verified normal UI106973/Dates14463,
   protected files/policies and read-only root. Never replay this consumed test.
2. Display-sleep capsule110500 FAILED its wake deadline. Read-only checks after
   Wi-Fi returned verified exact normal base UI112106/Dates14463, settings,
   policies, read-only root and no experimental host/drop/lock/owner cgroups.
   Its sleeping receipt arrived2074ms after the request, beyond the helper's
   1900ms limit; no wake batch was attempted. Never replay the consumed capsule.
   Corrected single-use152000 subsequently PASSED native display sleep/wake,
   original-page restoration and all four saved shapes. Normal UI129176/Dates14463
   independently verified restored, with exact settings/base/policies/root-ro.
   Never replay either capsule. See playbook/log/display_sleep_2026-09-23.md.
   Already-parked display sleep is not in-flight sleep or CPU suspend qualification.
3. The local visibility latch preserves old geometry until park/detachment;
   visibility-only loss, hidden-primary suspension, landscape wake and fresh
   publication pass local tests. Native boundary/eraser, in-flight sleep,
   lock/privacy, delayed HWC/keyboard and extension gates remain separate.
4. Remaining native acceptance is deferred by the explicit hands-on-pilot request.
   Keep Move untouched and no recurring-install entry yet.

* Qualification history (not current next actions)
0. Latest stopping point2026-09-23: ordinary-control cohort passed independent
   review and local checks (67Qt,26saved-file verifier,16stager tests). Exact
   trial20260923T001500Z-1 was staged/prepared on Pro, with a Mac-verified recovery
   archive. The activation command was rejected BEFORE execution because the
   Codex automatic approval reviewer hit an account usage limit. No runtime
   activation or remote backup-verification marker write occurred in that command.
   Local stager clearance was already consumed; do not replay it or bypass approval.
   Last normal state was UI89759/Dates14463, all protected hashes unchanged/rootro.
   The approval error subsequently cleared through the same require_escalated
   process. A read-only retry verified UI89759/no restarts and no owner, backup
   marker or lock: the rejected command had not run. The single prepared attempt
   was then launched normally with the same verified backup/manifest; no stager
   replay or approval bypass. Native host loading then refused SizeRuler's
   unsupported Accessible attached type, before any host-ready, test-note creation
   or ink. Independent automatic recovery verified normal UI95131/Dates14463,
   protected settings/base/policies/rootro and no surviving Companion/owner.
   Fix: strip four desktop-only accessibility bindings from the tablet ruler;
   fail-fast scans now recognize host/type load errors. Next: one separately
   reviewed corrected attempt, then page/tool/lifecycle qualification.
   No ordinary-control hardware pass or everyday installation is yet claimed.
0. 2026-09-23 actual progress: bootstrap admission trial20260922T233500Z-1
   PASSED real native startup, cold worker discovery, four correctly attributed
   strokes, during-stroke seal/park and1080-to1440resize. All four saved shapes
   match exactly after recovery. Normal Pro base86380/Dates14463 restored and
   independently verified. Both admission clearances are consumed.
   Ordinary open/resize/tuck/close now use a local generation-tagged transaction
   adapter, preserving old input until native park and waiting for final native
   geometry/candidates before release. The first-statement stock-open guard
   preserves BetterTOC in all three composition orders. Next: saved disposable
   native reopen/visible pixel occlusion, then ordinary lifecycle qualification.
   Keep ordinary ink off until that evidence exists; no Move or firmware changes.
   All62desktop QML tests pass, including25ordinary-host boundary/pointer tests.
   Additional transaction regressions cover old-controller changes, stale/native
   notifications, retained availability loss, loading and publication failure.
   Visual trial20260922T235000Z-1 is separately reviewed, prepared with a verified
   Mac recovery backup and consumed one-run clearance. Its read-only helper
   copies only visible pixels, not48padding bytes per row. No pen is injected.
   Actual visualtrialPASSED: bothsavednotebooks reopened, settledframes show
   correcttemplates/strokes/occlusion; primaryfirst720rows byte-identical atboth
   heights. Independent recovery restored normal UI89759/Dates14463 with all
   protected hashes unchanged. Next: exercise the actual ordinary controls and
   transaction adapter on disposable notebooks, not another frozen-host repeat.
   The actual-control diagnostic is implemented locally: choose/pick, half and
   two-thirds presets, four native stroke submissions, tuck/reveal and chooser
   cancellation, then an owned final park through autosave. Its separate controller
   preserves the same recovery/release budgets and includes the SizeRuler payload.
   Local three-order composition and actual-control QML tests pass. The saved-file
   verifier distinguishes this profile and rejects missing or reordered lifecycle
   evidence. Next: reviewed bounded hardware attempt and independent recovery;
   the ordinary package remains pen-disabled until actual gates pass.
0. 2026-09-23 continuation: the user again requested action. Fresh strict-key
   read-only checks match Ferrari3.29.0.148, accepted base/settings/policies,
   UI79742/Dates14463 with zero restarts and no competing owner. Prepare a fresh
   exact bootstrap-based disposable admission capsule; keep the old combined
   stager consumed. The existing test already gates input on successful native
   startup/cold-worker readiness, so do not add a redundant startup-only restart.
   No physical recovery confirmation or product acceptance is inferred from
   service health. Ordinary/personal-document writing remains gated off.
   Two independent exact-artifact reviewers cleared one always-reverting
   disposable attempt20260922T233500Z-1 after202Node tests/three-order composition
   and real target isolation smoke. Following the user's repeated autonomous
   continuation request, proceed under fresh machine recovery/backup gates;
   do not call this physical acceptance or loosen release requirements.
0. 2026-09-23 startup fix: recovered Memfault stacktrace matches the exact native
   build and places the abort in numeric parsing of a QProcess helper's stdout.
   The old combined preload leaks Qt into every exec child, making GUI-extension
   activation/stdout pollution a concrete hypothesis. Replace it with a Qt-free,
   canonical-executable-scoped bootstrap and a resident versioned core bridge.
   Cross-ELF dependency checks and the bounded standalone target audit pass,
   including inherited helper isolation, absent-core foreign process, old combined
   preload negative control and full fake-worker/QML transaction. No XOVI or UI
   restart was used. UI79742/Dates14463 and protected settings remained unchanged;
   the independent postcheck found no audit cgroup or active Companion. This is
   not a Companion installation or proof of native helper output. Preserve
   the consumed UI-trial block, physical recovery confirmation requirement and
   fresh exact-artifact review before another UI trial. The new controller builds
   separately without changing consumed artifacts; 189 Node tests and the Qt
   admission suite pass. Continue ordinary-host
   integration only after native admission and visual gates pass.
0. 2026-09-23: implement cold-start public Qt self-move observation plus queued
   native worker parking for preset transitions. Independent exact-binary review
   supports the corrected detach/cache-empty/final-publication ordering. Test the
   resident sidecar locally and in a standalone target process before any UI
   trial; then wire geometry transactions and qualify only on disposable notes.
   Ordinary writing remains disabled until the complete path passes.
   The hardened resident module passes 24 Qt test cases (including 11 isolated
   failure cases), normal and address/undefined-sanitizer runs. Exact-target
   standalone preload/QML smoke passed with UI76210/Dates14463 unchanged and
   zero restarts. New admission diagnostic composes in three orders with all
   30 resources. One independently reviewed UI trial20260922T221000Z-1 aborted
   at startup with std::invalid_argument/stoi before any Companion host-ready,
   worker-ready, disposable creation or scripted-ink receipt. The cause was not
   established then; see the later startup fix above. Watchdog recovery and fresh read-only verification restored
   UI79742/Dates14463 with zero restarts, exact protected settings/base/policies,
   read-only root and no active Companion runtime. Move remains untouched.
   Clearance is consumed and the stager refuses all further runs. Next: local
   startup-cause isolation; obtain normal-input recovery confirmation and a fresh
   exact-artifact review before another bounded device trial. No ordinary
   installation is ready. See playbook/log/admission_probe_2026-09-23.md.
0. Current direction: implement the tap-only preset ruler on the current Pro
   branch; preserve historical diagnostic builds. Qualify discrete pen handoff
   and native visuals before enabling the ordinary host's writing gate. Fixed
   sizes alone do not prove that pending strokes survive a layout change.
   Ruler implementation and local pointer tests now pass; all consumed native
   diagnostic hashes remain unchanged. Direct producer pen-up filtering is a
   promising discrete-transition candidate (see ADMISSION-HANDOFF.md), not yet
   implemented/qualified. No matching Pro SSH endpoint was found on the current
   subnet, so this revision made no tablet changes. Next: finish the concrete
   cold-start/transition integration, then a fresh bounded native test when
   reachable; do not install the pen-disabled candidate as a working feature.
1. ReManager handover and fresh strict-key identity/inventory verification passed.
2. Load-only trial passed and automatically restored the accepted r1 base at
   UI22703 / Dates14463. Native writing remains disabled; no active Companion.
3. The bounded, independently reviewed rendering trial uses two
   disposable documents. Candidate already disables direct framebuffer paths
   while paired and clips native input; actual behavior must be verified.
   The diagnostic creates only labeled test notebooks through native APIs,
   blocks all pen input throughout its lifetime, limits UI memory, captures only
   disposable views, and restores the prior view before automatic base recovery.
   Local review passed (45 Node / 29 Qt tests; both composition profiles pass).
   First trial timed out at readiness and recovered base UI28522 / Dates14463.
   The empty-library-view readiness correction passes 53 Node / 29 Qt tests;
   independent review passed. Second trial opened both native documents, then
   failed on a native API argument error; recovered base UI34683 / Dates14463.
   Native refresh correction is local only (55 Node / 29 Qt tests pass).
   User confirmed normal scrolling returned and authorized continuation.
   Refresh fix, fail-fast watchdog and visible temporary-test notice now have
   independent review (66 Node / 29 Qt tests); use a fresh guarded trial and backup.
   Third trial passed movement/focus but Qt rejected capture on an internally
   C++-created viewport (no QML engine). Fast recovery restored UI37973 / Dates14463.
   Fourth trial's QML-created SceneView capture caused native SIGSEGV after
   unsupported-layer warnings; watchdog restored UI41100 / Dates14463. No visual
   pass. Render staging is now suspended in code. Investigate a non-layer-based
   diagnostic locally and obtain fresh independent review before any new trial.
   Keep all writing disabled; do not replay old stages.
   Independent review recommends a no-capture structural diagnostic and separate
   visual acceptance restricted to disposable pages. Do not substitute a parent
   grab or whole-screen stream. A separate structural profile is now implemented
   locally: same input locks and recovery, no capture APIs, repeated object/ID/
   geometry guards, bounded observation, exact return and nonvisual-only receipt.
   Independent review passed for its exact four files. Structural staging is
   pinned to those bytes for one bounded always-revert trial; historical render
   stays blocked. Fresh device/base/backup gates remain mandatory.
   Local suite passes (86 Node / 34 Qt; three profiles x three orders x 29 QML
   resources). Initial discovery failed; after the user woke the Pro, the one
   reviewed structural run passed and automatically restored UI45382 / Dates14463,
   with all settings/QMD/policy checks passing. No capture, ink or visual pass.
   User subsequently requested continuation. Work on remaining concrete native
   constraints; the normal base stays active between separately reviewed trials.
   Do not promote mere structural success into writing or visual qualification.
4. Qualify direct native surface hit-testing, pen/save durability, shared plugin
   ownership and fluidity before enabling writing or declaring a personal pilot.
   Before ink, also prove every part of a page can scroll into each short exposed
   pane: the fixed full-height native viewport's paper bounds may otherwise hide
   lower content. Native gesture ownership alone is not this acceptance test.
5. Exposed-pane navigation and retained-manager input refresh are implemented
   locally. A separate pen-disabled geometry profile now has independent
   First geometry run passed upper-pane scrolling, then refused a later check
   and automatically restored base UI49237 / Dates14463. That clearance is spent.
   Loading/readiness delta at 3a7bf6d has new one-run independent clearance (41 Qt,
   three-order normal and geometry composition). Execute only its exact four
   pinned artifacts with fresh backup and automatic base recovery. This does not
   qualify ink, visuals, save durability or release.
   The new run 20260921T224844Z-1 PASSED: both panes reach page edges at scale1,
   both input transforms refresh after loading, tuck/reopen and exact return pass.
   Automatic recovery restored UI53104 / Dates14463 and all settings/base hashes.
   Its one-run clearance is consumed and further geometry staging is blocked.
6. Resolve native stroke-lifetime/document-transition holds before any ink profile;
   see playbook/INK-QUALIFICATION.md. Tuck-transition eligibility is fixed locally.
   Exact-build audit found native destructor synchronization but no proven exposed
   per-stroke drain/save acknowledgement in inspected paths. Stop before enabling
   ink under the user's low-risk constraint; Pro base53104 remains restored.
   Future work needs a safe pre-mutation handoff design, not a longer quiet timer.
7. New user authorization supersedes the blanket pre-ink stop for a disposable-only
   fixed-layout test. The local ink diagnostic keeps both native panes eligible
   simultaneously, locks all layout/document transitions, bounds event injection,
   and independently releases the pen before base recovery. No personal pilot.
   Complete local failure tests and narrow independent review, then run once
   only on the freshly identified Pro with a verified backup. Inspect the two
   exact saved test notebooks after recovery; a submission log is not durability.
   Initial network discovery this turn did not find the expected Pro fingerprint.
   The user subsequently woke the tablet: strict-key identity and exact base
   rechecks succeeded (same UI53104/Dates14463, build20260911125116, all11QMDs and
   three service policies unchanged, root read-only). Complete the isolated ink
   run rather than stopping at local test preparation. ReManager is implementing
   the bounded input helper while this task owns all device access/recovery.
   Helper handback is complete: hard8s/2s alarms, bounded nonblocking IO, sticky
   unexpected-event detection, actual-C failure tests; recovery now proves the
   whole owner cgroup empty before pen release. Verify the revised frozen bytes,
   then execute the one disposable-only test. No simulated tablet outcome.
   Live run20260922T043500Z-1 PASSED native two-pane submissions without selection:
   each31-point callback reached its own controller, helper released, automatic
   recovery restored base60746/Dates14463 with all11QMD/10settings/3policies intact.
   Both exact saved files contain one24-point stroke with matching shape/bounds.
   Exact sample preservation, native reopen of both notes, visual clipping and
   fluid dynamic use remain unqualified. One-run ink clearance is consumed.
8. Continue toward safe live layout transitions. Investigate actual native handler
   retirement/recreation using its synchronized destructor while keeping document
   controllers alive; do not substitute a quiet timer for worker completion or
   make opaque binary/firmware modifications. Fixed-layout native ink is feasible.
   Local C++/QML negative regression confirms Component.onDestruction precedes
   native derived destruction. An observer-only public-Qt module is being built
   with ReManager; it will acknowledge two native QObject destructions without
   owning/deleting handlers or touching worker internals. An acknowledgement is
   not a save or general input-queue drain. No new tablet payload is deployed.
   Implemented a separate dynamic-handler retirement diagnostic with independent
   public-Qt observer, exact Linux ELF checks, two distinguishable stroke rounds,
   native lifetime acknowledgement before movement, and four-shape readback.
   Local state-machine,35C++ observer,11Python saved-file,50Qt UI and three-order
   full-stack composition checks pass. The Pro went offline during local work;
   normal base60746/Dates14463 was last verified unchanged. Standalone target
   module import and then the separately reviewed UI trial are next. Keep the
   prepared files local until fresh target identity/base checks succeed.
   The immediate-install retry found the Mac on a new192.168.86.0/24 network,
   no SSH endpoints after two scans, and no USB network interface. Current Pro
   IP was requested; no target smoke or UI trial has run. Resume directly at the
   standalone import preflight when its host key is found; do not rebuild or
   repeat the already passed frozen-code review without a source change.
9. Later reconnect succeeded at10.100.102.101 with the exact accepted base.
   The actual standalone observer import passed with both service PIDs unchanged.
   First retirement trial20260922T171005Z-1 stopped on an unexpected stroke
   callback before scripted input started; retirement/movement were not tested.
   Normal base71641/Dates14463 and all protected hashes were verified restored.
   Two new test pages contain no saved lines; cause/provenance is unproven.
   Local follow-up now validates/logs before native submission and moves the
   stability observation to after input closes. A two-instance desktop factory
   fixture passes. Review the exact revised bytes, then use one new bounded
   disposable trial; do not interpret either failure or local success as release.
   The revised exact QMD/host/controller have independent one-run clearance,
   including a repeated strict-error scan after the moved stability interval.
   Complete full regressions and fresh device/backup gates before the new trial.
   Trial20260922T173005Z-1 completed native write-retire-move-recreate-write:
   four correctly attributed31-point submissions. The final controller stability
   loop hit its deadline; no whole-trial pass marker. Watchdog restored base76210
   with Dates14463 and every protected hash intact. Readback has two24-point
   lines per disposable note, but the pre-controller rectangle differs by an
   exact3-unit inset from saved centerlines; audit semantics before claiming
   persistence. This one-run review is consumed. Do not repeat it unchanged.
   Exact-native audit resolved this as conservative tool/thickness padding.
   The corrected strict verifier reconstructs all four rectangles exactly and
   verifies one-to-one saved shapes. Unknown tools/thicknesses are refused;
   the5-unit tolerance was not widened. This is a saved-shape pass, not an
   overall controller, native-reopen, visual or release pass.
10. Resolve the rectangle semantics, then qualify native reopen and visual
    occlusion using only these disposable documents. The already-loaded
    framebuffer-spy provides a possible read-only pixel source without Qt layers
    or a network screen-sharing server; no capture is authorized by this design
    note alone. General user-driven lifecycle handoff remains separate work.
    The pen-disabled visual-reopen capsule now builds and passes16 local tests
    plus all3 composition orders. It is unexecuted, has no staging clearance,
    and does not solve input admission. Do not run it merely to postpone the
    core lifecycle decision.
11. Exact native/public-Qt audit establishes a useful queued worker completion
    acknowledgement, but no complete owner-consistent producer admission closure
    for arbitrary seamless user dragging. Blocking producer reads invalidate a
    queued producer-dispatcher closure; an atomic filter alone races a report
    already past its check. Manual region signals disagree with native cache
    ownership. See playbook/ADMISSION-HANDOFF.md. Ordinary ink stays disabled;
    no personal installation is authorized by the four-stroke result. A future
    continuation needs a concrete admission protocol, not another identical test.
    Final refinement: manual empty-region publication DOES seal the producer
    while preserving existing records. However signal-blocked manager updates
    can still alter handlers' pending clips, and normal reopening publishes
    producer acceptance before fresh worker candidates. The proposed lightweight
    stop/move/reopen protocol is therefore not cleared; do not describe the
    narrower producer-seal result itself as impossible.
    Cold-start worker discovery before a genuine completion is also unresolved;
    inspected PenInput/manager APIs expose no worker pointer/startup notification.
    Do not require or synthesize a personal-note setup stroke as an implicit fix.

* Implementation evidence
- Local native adapter, pairing store, picker and exact-version QMD exist.
- Offline three-order full-stack composition passes; no qualified tablet release yet.
- Native candidate writing remains disabled. Qt mocks do not qualify native ink.
- ReManager explicitly handed over after restoration. Read-only checks matched
  the accepted runtime. Companion remains unqualified and separate from its
  installed 11-plugin base; all trials need their own inventory and controller.
* Quick Pad revised requirements (2026-09-24)
- Flush bottom-right by default, configurable bottom-left; thin boundaries only.
- Handwriting on both exposed source and pad, with Companion's shared tools.
- Reuse native Companion document/pen lifecycle with native full-page zoom.
- Keep accepted native task-list glyph; toolbar tap opens/closes quickly.
- Reduce open/close flashes without bypassing native input synchronization.
** Superseded requirements
- Inset/scaled card, large pen-here header, and source read-only restriction are
  rejected by user feedback. Native pen alignment needs hands-on confirmation.
** Immediate next steps
- Install the guarded corner-viewport revision and collect handwriting/refresh
  feedback. Do not describe desktop tests as physical pen qualification.
