#+title: Companion Notebook

* Objective
Read and write without losing context or compromising native handwriting.

* Accepted requirements (2026-09-21)
- Portrait-first bottom pull-out writable margin overlays the main document.
- Main viewport and page scale do not shrink as the margin is pulled.
- Dragging updates the actual margin continuously, with no settle-on-release substitute.
- Two independently scrollable documents; finger tap selects the writing pane.
- A pen tap on an inactive pane selects it but does not ink; subsequent strokes write.
- Active pane has a visible writing indicator. Scrolling does not change focus.
- Pairings belong to the primary notebook or PDF; no same-document pair in v1.
- Tucking preserves positions and pairing; detaching never deletes documents.
- No layout/focus/document changes during a stroke; gestures keep their owner.
- Prototype is local/offline. Native ink must use native controllers and saves.
- Pro only initially; no changes to Move or shared servers.

* Requirements changed
- Earlier proposal displaced/resized the main viewport: replaced by slide-over.
- Automatic focus on first stroke: replaced by explicit tap-to-select.
- Same-notebook different locations: excluded from v1.
- Landscape in v1: deferred. Later landscape should fit scaled documents to equal halves.
- Divider-only preview fallback: rejected; live fluid movement is a hard gate.
- Design-only phase: user authorized implementation, with original device safety constraints.

* Validation and release decision
- Desktop controller and QML tests must pass, including pointer drag before release.
- Hardware must prove independent rendering, clipping, pen ownership and native durability.
- Base 3.29 XOVI/QRR qualification precedes any native probe.
- Any lost, duplicate or wrong-document ink blocks release.
- Measure high-speed-video drag latency and visual artifacts on the Pro; compare
  with stock touch response and obtain actual user acceptance of fluidity.
- No claim of native support or inclusion in reinstalls before these gates pass.

* Next steps
1. ReManager handover and fresh strict-key identity/inventory verification passed.
2. Load-only trial passed and automatically restored the accepted r1 base at
   UI22703 / Dates14463. Native writing remains disabled; no active Companion.
3. The bounded, independently reviewed rendering trial uses two
   disposable documents. Candidate already disables direct framebuffer paths
   while paired and clips native input; actual behavior must be verified.
   The diagnostic creates only labeled test notebooks through native APIs,
   blocks all pen input throughout its lifetime, limits UI memory, captures only
   disposable views, and restores the prior view before automatic base recovery.
   Local review passed (45 Node / 29 Qt tests; both composition profiles pass).
   First trial timed out at readiness and recovered base UI28522 / Dates14463.
   The empty-library-view readiness correction passes 53 Node / 29 Qt tests;
   independent review passed. Second trial opened both native documents, then
   failed on a native API argument error; recovered base UI34683 / Dates14463.
   Native refresh correction is local only (55 Node / 29 Qt tests pass).
   User confirmed normal scrolling returned and authorized continuation.
   Refresh fix, fail-fast watchdog and visible temporary-test notice now have
   independent review (66 Node / 29 Qt tests); use a fresh guarded trial and backup.
   Third trial passed movement/focus but Qt rejected capture on an internally
   C++-created viewport (no QML engine). Fast recovery restored UI37973 / Dates14463.
   Fourth trial's QML-created SceneView capture caused native SIGSEGV after
   unsupported-layer warnings; watchdog restored UI41100 / Dates14463. No visual
   pass. Render staging is now suspended in code. Investigate a non-layer-based
   diagnostic locally and obtain fresh independent review before any new trial.
   Keep all writing disabled; do not replay old stages.
   Independent review recommends a no-capture structural diagnostic and separate
   visual acceptance restricted to disposable pages. Do not substitute a parent
   grab or whole-screen stream. A separate structural profile is now implemented
   locally: same input locks and recovery, no capture APIs, repeated object/ID/
   geometry guards, bounded observation, exact return and nonvisual-only receipt.
   Independent review passed for its exact four files. Structural staging is
   pinned to those bytes for one bounded always-revert trial; historical render
   stays blocked. Fresh device/base/backup gates remain mandatory.
   Local suite passes (86 Node / 34 Qt; three profiles x three orders x 29 QML
   resources). No device run: Pro did not respond and no matching host key was
   found on the current subnet. Keep normal tablet state untouched.
4. Qualify inactive pen-tap routing, native pen/save durability, shared plugin
   ownership and fluidity before enabling writing or declaring a personal pilot.
   Before ink, also prove every part of a page can scroll into each short exposed
   pane: the fixed full-height native viewport's paper bounds may otherwise hide
   lower content. Native gesture ownership alone is not this acceptance test.

* Implementation evidence
- Local native adapter, pairing store, picker and exact-version QMD exist.
- Offline three-order full-stack composition passes; no qualified tablet release yet.
- Native candidate writing remains disabled. Qt mocks do not qualify native ink.
- ReManager explicitly handed over after restoration. Read-only checks matched
  the accepted runtime. Companion remains unqualified and separate from its
  installed 11-plugin base; all trials need their own inventory and controller.
